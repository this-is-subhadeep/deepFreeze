import { DepositValidatorDirective } from './deposit-validator.directive';

describe('DepositValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new DepositValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
